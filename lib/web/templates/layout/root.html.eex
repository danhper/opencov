<!DOCTYPE html>
<html lang="en">
  <head>
      <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <%= csrf_meta_tag() %>
    <%= live_title_tag assigns[:page_title] || "LibreCov" %>
    <link phx-track-static rel="stylesheet" href="<%= Routes.static_path(@conn, "/vendor.css") %>" />
    <link phx-track-static rel="stylesheet" href="<%= Routes.static_path(@conn, "/theme.css") %>" />
    <link phx-track-static rel="stylesheet" href="<%= Routes.static_path(@conn, "/app.css") %>" />
    <script phx-track-static defer type="text/javascript" src="<%=Routes.static_path(@conn, "/vendor.js") %>"></script>
    <script phx-track-static defer type="text/javascript" src="<%= Routes.static_path(@conn, "/app.js")%>"></script>
  </head>
  <body class="librecov">
    <div class="container-fluid body-wrapper">
    <%= render "header.html", conn: @conn %>

  <div class="container">
    <%= if get_flash(@conn, :info) do %>
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
    <% end %>
    <%= if get_flash(@conn, :error) do %>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
    <% end %>
  </div>
    <%= @inner_content %>
  </div>

  </body>
</html>
