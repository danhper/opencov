apiVersion: v1
kind: Service
metadata:
  name: "opencov-http-{{.CurrentEnvironment}}"
spec:
  selector:
    project: opencov
    service: http
    env: {{.CurrentEnvironment}}
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
      nodePort: {{.NodePort}}
  type: NodePort
